{
  "compilerOptions": {
    "strict": true // 开启所有严格类型检查
    // 可选：如果需要，可以单独调整部分规则，例如：
    // "noImplicitAny": true,
    // "strictNullChecks": true
  },
  "files": [],
  "references": [
    { "path": "./tsconfig.app.json" },
    { "path": "./tsconfig.node.json" }
  ]
}
